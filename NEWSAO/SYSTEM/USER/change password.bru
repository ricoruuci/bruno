meta {
  name: change password
  type: http
  seq: 6
}

patch {
  url: {{API_URL}}/change-password/1
  body: json
  auth: inherit
}

body:json {
  {
    "old_password" : "Q5LCC4",
    "new_password" : "admin"
  }
}
