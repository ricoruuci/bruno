meta {
  name: create user
  type: http
  seq: 4
}

post {
  url: {{API_URL}}/user
  body: json
  auth: inherit
}

body:json {
  {
    "userid" : "robby",
    "password" : "bngksrobby",
    "group_user" : "ADM",
    /* group user hardcode = ADM USR */
    "company_id" : "2"
    /* ambil dari list-cabang */
  }
}

settings {
  encodeUrl: true
}
