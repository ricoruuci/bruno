meta {
  name: create user
  type: http
  seq: 4
}

post {
  url: {{API_URL}}/user
  body: json
  auth: inherit
}

body:json {
  {
    "userid" : "faiz",
    "password" : "bngksrobby",
    "group_user" : "LAP",
    /* group user hardcode = ADM USR */
    "company_id" : "1"
    /* ambil dari list-cabang */
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
