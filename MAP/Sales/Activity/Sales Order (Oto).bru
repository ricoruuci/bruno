meta {
  name: Sales Order (Oto)
  type: http
  seq: 4
}

patch {
  url: {{API_URL}}/otorisasiso
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "soid": "SOM-MAP/202411/0001",
      "jenis": "Y"
  }
}
