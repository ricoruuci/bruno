info:
  name: get user by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/user-by-id"
  body:
    type: json
    data: |-
      {
        "email" : "admin@wwp.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
