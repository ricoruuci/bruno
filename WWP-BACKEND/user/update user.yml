info:
  name: update user
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{API_URL}}/user"
  body:
    type: json
    data: |-
      {
          "email": "lawyer@wwp.com",
          "name": "John Updated",
          "client_id": "CL-002",
          "role": "Admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
