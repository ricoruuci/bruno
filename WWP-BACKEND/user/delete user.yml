info:
  name: delete user
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_URL}}/user"
  body:
    type: json
    data: |-
      {
          "email": "lawyer@wwp.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
