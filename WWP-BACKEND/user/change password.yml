info:
  name: change password
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{API_URL}}/change-password"
  body:
    type: json
    data: |-
      {
          "email": "lawyer@wwp.com",
          "old_password": "123456",
          "new_password": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
