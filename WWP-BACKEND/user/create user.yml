info:
  name: create user
  type: http
  seq: 4

http:
  method: POST
  url: "{{API_URL}}/user"
  body:
    type: json
    data: |-
      {
          "email": "lawyer@wwp.com",
          "name": "John Doe",
          "client_id": "CL-001",
          "role": "Lawyer",
          "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
