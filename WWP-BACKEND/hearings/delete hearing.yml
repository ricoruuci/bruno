info:
  name: delete hearing
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_URL}}/hearing"
  body:
    type: json
    data: |-
      {
          "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
