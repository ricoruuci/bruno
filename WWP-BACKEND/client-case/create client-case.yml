info:
  name: create client-case
  type: http
  seq: 4

http:
  method: POST
  url: "{{API_URL}}/client-case"
  body:
    type: json
    data: |-
      {
        "client_id": 1, 
        "case_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
