info:
  name: Logout
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/logout"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
