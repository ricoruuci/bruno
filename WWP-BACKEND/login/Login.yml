info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/login"
  body:
    type: json
    data: |-
      {
        "email" : "admin@wwp.com",
        "password" : "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
