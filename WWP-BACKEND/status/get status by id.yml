info:
  name: get status by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/status-by-id"
  body:
    type: json
    data: |-
      {
          "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
