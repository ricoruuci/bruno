info:
  name: update status
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{API_URL}}/status"
  body:
    type: json
    data: |-
      {
          "id": 1,
          "kode": "ST-001",
          "nama": "Open Updated",
          "keterangan": "-"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
