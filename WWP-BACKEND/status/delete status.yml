info:
  name: delete status
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_URL}}/status"
  body:
    type: json
    data: |-
      {
          "id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
