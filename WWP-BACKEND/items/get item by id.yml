info:
  name: get item by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/item-by-id"
  body:
    type: json
    data: |-
      {
          "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
