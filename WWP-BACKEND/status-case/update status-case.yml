info:
  name: update status-case
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{API_URL}}/status-case"
  body:
    type: json
    data: |-
      {
        "id": 1, 
        "status_case": 1, 
        "case_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
