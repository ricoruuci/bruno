info:
  name: delete invoice
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_URL}}/invoice"
  body:
    type: json
    data: |-
      {
          "id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
