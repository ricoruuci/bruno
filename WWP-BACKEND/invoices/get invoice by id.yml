info:
  name: get invoice by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/invoice-by-id"
  body:
    type: json
    data: |-
      {
          "id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
