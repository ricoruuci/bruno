info:
  name: get lawyer-case by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/lawyer-case-by-id"
  body:
    type: json
    data: |-
      {
          "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
