info:
  name: delete category
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_URL}}/category"
  body:
    type: json
    data: |-
      {
          "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
