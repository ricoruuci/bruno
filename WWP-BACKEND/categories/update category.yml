info:
  name: update category
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{API_URL}}/category"
  body:
    type: json
    data: |-
      {
          "id": 1,
          "kode": "CT-001",
          "nama": "Pidana Khusus",
          "keterangan": "Kasus pidana khusus - updated"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
