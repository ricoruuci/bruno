meta {
  name: create grouprek
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/grouprek
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "grouprekname" : "cogs",
      "kode" : "1"
  }
}
