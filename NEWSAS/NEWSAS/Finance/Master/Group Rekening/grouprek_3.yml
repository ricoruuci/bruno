info:
  name: grouprek_3
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_URL}}/grouprek"
  body:
    type: json
    data: |-
      {
          //"grouprekid" : "1.003",
        "grouprekidkeyword" : "",
        "groupreknamekeyword" : "",
        "sortby" : "id"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
