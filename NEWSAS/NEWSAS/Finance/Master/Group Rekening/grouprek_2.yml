info:
  name: grouprek_2
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_URL}}/grouprek"
  body:
    type: json
    data: |-
      {
          "grouprekid": "1.002"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
