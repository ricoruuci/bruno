info:
  name: grouprek_1
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{API_URL}}/grouprek"
  body:
    type: json
    data: |-
      {
          "grouprekid": "1.002",
          "grouprekname" : "cogs"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
