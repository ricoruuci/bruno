info:
  name: rekening insert
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/rekening"
  body:
    type: json
    data: |-
      {
        "rekeningname"  : "tes",
        "grouprekid": "110100",
        "tipe" : "1",
        "note" : "tes note",
        "fgactive" : "Y"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
