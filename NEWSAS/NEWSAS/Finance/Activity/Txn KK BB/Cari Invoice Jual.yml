info:
  name: Cari Invoice Jual
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/txnkkbb/cariinvoicejual"
  body:
    type: json
    data: |-
      {
          "custid": "UE032",
          "saleidkeyword" : ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
