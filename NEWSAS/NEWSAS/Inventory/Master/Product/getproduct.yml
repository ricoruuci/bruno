info:
  name: getproduct
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/product"
  body:
    type: json
    data: |2
      
        {
      //     "productid" : "31",
          "productidkeyword" : "",
          "productnamekeyword" : ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
