info:
  name: Sales_3
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{API_URL}}/sales"
  body:
    type: json
    data: |-
      {
         "salesid" : "S002"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
