info:
  name: Customer_3
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{API_URL}}/customer"
  body:
    type: json
    data: |-
      {
              "custid": "DS022"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
