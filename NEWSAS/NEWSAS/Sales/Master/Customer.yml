info:
  name: Customer
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/customer"
  body:
    type: json
    data: |-
      {
         //"custid": "DA002",
         "custidkeyword": "A",
          "custnamekeyowrd": "",
          "sortby": "custname"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
