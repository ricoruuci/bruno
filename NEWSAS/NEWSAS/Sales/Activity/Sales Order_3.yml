info:
  name: Sales Order_3
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{API_URL}}/salesorder"
  body:
    type: json
    data: |-
      {
          "soid": "SOM-MAP/202411/0001"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
