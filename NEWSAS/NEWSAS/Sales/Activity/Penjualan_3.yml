info:
  name: Penjualan_3
  type: http
  seq: 24

http:
  method: DELETE
  url: "{{API_URL}}/penjualan"
  body:
    type: json
    data: |-
      {
          "saleid" : "1Tech.24.10.0001"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
