info:
  name: Penjualan Cari FPS
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/penjualan/carifps"
  body:
    type: json
    data: |-
      {
          "taxidkeyword": ""

      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
