info:
  name: Penjualan Cek Bayar
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/penjualan/cekbayar"
  body:
    type: json
    data: |-
      {
          "saleid": "MAP-AR/2402/00001"

      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
