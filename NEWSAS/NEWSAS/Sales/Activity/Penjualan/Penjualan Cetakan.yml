info:
  name: Penjualan Cetakan
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/penjualan/format"
  body:
    type: json
    data: |-
      {
          "saleid": "MAP-AR/2411/00002"

      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
