info:
  name: Sales Order (Oto)
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{API_URL}}/otorisasiso"
  body:
    type: json
    data: |-
      {
          "soid": "SOM-MAP/202411/0002",
          "jenis": "Y"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
