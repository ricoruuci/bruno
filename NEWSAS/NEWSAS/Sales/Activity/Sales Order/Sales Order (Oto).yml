info:
  name: Sales Order (Oto)
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{API_URL}}/otorisasiso"
  body:
    type: json
    data: |-
      {
          "soid": "SO-1Tech/2002/0001",
          "jenis": "Y"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
