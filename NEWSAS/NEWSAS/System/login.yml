info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/login"
  body:
    type: json
    data: |2+
       {
           "userid" : "admin",
           "password" : ""
       }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
