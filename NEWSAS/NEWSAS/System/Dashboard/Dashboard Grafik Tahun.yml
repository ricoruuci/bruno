info:
  name: Dashboard Grafik Tahun
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/dashboard/tahun"
  body:
    type: json
    data: "\n\n"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
