info:
  name: Dashboard Total
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/dashboard/total"
  body:
    type: json
    data: "\n\n"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
