info:
  name: Dashboard Grafik
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/dashboard"
  body:
    type: json
    data: "\n\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
