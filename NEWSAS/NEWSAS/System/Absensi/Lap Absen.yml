info:
  name: Lap Absen
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_URL}}/laporanabsen"
  body:
    type: json
    data: |2+
       {
           "userid" : "rico",
           "dari" : "20230101",
           "sampai" : "20231031"
       }

  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
