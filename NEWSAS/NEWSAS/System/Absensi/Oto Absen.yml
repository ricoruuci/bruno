info:
  name: Oto Absen
  type: http
  seq: 5

http:
  method: POST
  url: "{{API_URL}}/otoabsen"
  body:
    type: json
    data: |-
      { 
          "transid" : "2024R003002",
          "jenis" : "Y"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
