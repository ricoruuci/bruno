info:
  name: Oto Absen
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/otoabsen"
  body:
    type: json
    data: |-
      { 
          "transid" : "2026R003003",
          "jenis" : "Y"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
