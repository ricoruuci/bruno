info:
  name: Absen
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/absen"
  body:
    type: json
    data: |2+
       {
           "userid" : "rico",
           "keperluan" : "L",
           "keterangan" : "coba",
           "foto" : ""
       }

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
