info:
  name: security access_1
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/security"
  body:
    type: json
    data: |-
      {
          "kdgroup" : "engineer"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
