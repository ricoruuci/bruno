info:
  name: Dashboard Grafik
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_URL}}/dashboard"
  body:
    type: json
    data: "\n\n"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
