info:
  name: group user_2
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{API_URL}}/security/group"
  body:
    type: json
    data: |-
      {
          "kdgroup" : "admin"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
