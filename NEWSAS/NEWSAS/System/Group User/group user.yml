info:
  name: group user
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/security/group"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
