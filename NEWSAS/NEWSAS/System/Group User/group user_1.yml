info:
  name: group user_1
  type: http
  seq: 7

http:
  method: POST
  url: "{{API_URL}}/security/group"
  body:
    type: json
    data: |-
      {
          "kdgroup" : "admin"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
