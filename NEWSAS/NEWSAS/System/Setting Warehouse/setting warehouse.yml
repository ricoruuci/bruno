info:
  name: setting warehouse
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/setting"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
