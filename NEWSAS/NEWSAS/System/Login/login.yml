info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/login"
  body:
    type: json
    data: |2+
       {
           "userid" : "admin",
           "password" : ""
       }

runtime:
  variables:
    - name: API_URL
      value: http://127.0.0.1:8000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
