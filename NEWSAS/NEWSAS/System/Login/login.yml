info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/login"
  body:
    type: json
    data: |2+
       {
           "userid" : "ferdy",
           "password" : "1"
       }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
