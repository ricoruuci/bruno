info:
  name: logout
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/logout"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
