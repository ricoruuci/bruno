info:
  name: Otorisasi Pembelian
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/purchase/otorisasi"
  body:
    type: json
    data: |-
      {
          "purchaseid" : "ZJ24I01185"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
