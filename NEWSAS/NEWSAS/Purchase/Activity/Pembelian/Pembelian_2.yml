info:
  name: Pembelian_2
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{API_URL}}/pembelian"
  body:
    type: json
    data: |-
      {
          "konsinyasiinvid" : "AP-SAS/24/10/0004"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
