info:
  name: grn_3
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{API_URL}}/grn"
  body:
    type: json
    data: |-
      {
          "grnid": "1Tech/GR/2409/0081"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
