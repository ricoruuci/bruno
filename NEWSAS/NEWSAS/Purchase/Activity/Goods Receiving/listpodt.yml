info:
  name: listpodt
  type: http
  seq: 6

http:
  method: GET
  url: "{{API_URL}}/listpodt"
  body:
    type: json
    data: |-
      {
        "poid": "1Tech-PO/2020-070065"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
