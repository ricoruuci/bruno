info:
  name: Supplier
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/supplier"
  body:
    type: json
    data: |-
      {
         //"suppid": "A002",
         "suppidkeyword" : "",
         "suppnamekeyword" : "",
         "active" : "ya"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
